# Copy this file to .env.local and fill in the secret values before running the app.
# Example values below; DO NOT commit real secrets to source control.

# The URL your app runs on. If Next runs on a different port, adjust accordingly.
NEXTAUTH_URL=http://localhost:3002

# A long, random string used by NextAuth to sign cookies and tokens.
# Generate one locally with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=replace_with_a_random_long_hex_string

# Google OAuth credentials - obtain from Google Cloud Console.
# Redirect URI to register in Google Cloud (for local dev):
# http://localhost:3002/api/auth/callback/google
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Optional: if you add other providers or custom callbacks, add their secrets here.
# Copy to .env.local and set your Razorpay credentials
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
NEXT_PUBLIC_RAZORPAY_KEY_ID=
